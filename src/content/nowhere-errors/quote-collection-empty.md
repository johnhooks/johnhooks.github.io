---
code: nowhere.quote.collection_empty
title: wire empty
description: The quote wire has no quotes to connect.
---

The quote wire tried to start without any quotes in the collection.

That should not happen on the live site. The wire is built from local curated content, and
without that content there is no root, no branch, and no path to follow.

Elsewhere stops here because an empty archive cannot pretend to be a journey.
